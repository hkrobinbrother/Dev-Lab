let mixArr: (string | number)[] = ["eggs", 20]


// mixArr.push(true)


let coordination: [string, string] = ["husband", "wife"]

let nextCourse: [string, number] = ["next", 7]



const user: {
    firstName: string;
    readonly company: string;
} = { firstName: "hasan", company: "hep" }

// user.company = "hello"

console.log(user)